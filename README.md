# Installare torchvision 0.10.0 e flotramite pip 
```bash
  pip install -r requirements.txt
  ```
# può essere che manchi qualche libreria, ma non credo

# Aggiungere percorso a python
 ```bash
  python setup.py develop
  ```

